import type { IVendorOrganization } from "./organization-vendor.model.js";
import type { IVendor } from "@/modules/vendors/vendor.types.js";
import type { OrganizationVendorResponse, VendorRelationshipResponse } from "./organization-vendor.dto.js";
export function toOrganizationVendorResponse(item: IVendorOrganization, vendorOverride?: IVendor): OrganizationVendorResponse { const vendor = vendorOverride ?? item.vendorId as unknown as IVendor; return { id: item._id.toString(), vendorId: vendor._id.toString(), organizationId: item.organizationId.toString(), name: vendor.name, email: vendor.email, phone: vendor.phone, website: vendor.website, serviceCategories: vendor.serviceCategories ?? [], certifications: vendor.certifications, coverageRadiusKm: vendor.coverageRadiusKm, averageRating: vendor.averageRating, completedJobs: vendor.completedJobs, status: item.status, createdAt: item.createdAt.toISOString(), updatedAt: item.updatedAt.toISOString() }; }
export function toVendorRelationshipResponse(item: IVendorOrganization): VendorRelationshipResponse { return { vendorId: item.vendorId.toString(), organizationId: item.organizationId.toString(), status: item.status, createdBy: item.createdBy.toString(), activatedBy: item.activatedBy?.toString(), activatedAt: item.activatedAt?.toISOString(), suspendedBy: item.suspendedBy?.toString(), suspendedAt: item.suspendedAt?.toISOString(), removedBy: item.removedBy?.toString(), removedAt: item.removedAt?.toISOString() }; }
